# Copy this file to `.env` and set real values there.
# `.env` is git-ignored and is now the single source of truth for API keys.

# DeepSeek API (idea / script / VO narration generation)
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ElevenLabs API (voiceover generation)
ELEVENLABS_API_KEY=

# Google Flow automation
FLOW_HEADLESS=false
FLOW_EMAIL=
FLOW_PASSWORD=

# Pipeline defaults
DEFAULT_SCENE_COUNT=12
MAX_LLM_RETRIES=3
